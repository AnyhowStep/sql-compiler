CREATE FULLTEXT INDEX myIndex ON T (X) KEY_BLOCK_SIZE = 999;

-----
CREATE FULLTEXT INDEX `myIndex`
    ON `T`
    (`X` ASC)
    KEY_BLOCK_SIZE = 999
    LOCK = `DEFAULT`
    ALGORITHM = `DEFAULT`;

-----
[]
