CREATE FUNCTION FOO
    RETURNS STRING
    SONAME 'mylib.so';

CREATE FUNCTION FOO
    RETURNS INTEGER
    SONAME 'mylib.so';

CREATE FUNCTION FOO
    RETURNS REAL
    SONAME 'mylib.so';

CREATE FUNCTION FOO
    RETURNS DECIMAL
    SONAME 'mylib.so';

-----
CREATE FUNCTION `FOO`
    RETURNS STRING
    SONAME 'mylib.so';

CREATE FUNCTION `FOO`
    RETURNS INTEGER
    SONAME 'mylib.so';

CREATE FUNCTION `FOO`
    RETURNS REAL
    SONAME 'mylib.so';

CREATE FUNCTION `FOO`
    RETURNS DECIMAL
    SONAME 'mylib.so';

-----
[]
