CREATE FUNCTION FOO ()
    RETURNS BOOLEAN
    SQL SECURITY INVOKER
    SQL SECURITY DEFINER
    RETURN 1;

CREATE FUNCTION FOO ()
    RETURNS BOOLEAN
    SQL SECURITY DEFINER
    SQL SECURITY INVOKER
    RETURN 1;

-----
CREATE DEFINER = CURRENT_USER() FUNCTION `FOO` ()
    RETURNS BOOLEAN
    NOT DETERMINISTIC
    SQL SECURITY DEFINER
    RETURN 1;

CREATE DEFINER = CURRENT_USER() FUNCTION `FOO` ()
    RETURNS BOOLEAN
    NOT DETERMINISTIC
    SQL SECURITY INVOKER
    RETURN 1;

-----
[]
