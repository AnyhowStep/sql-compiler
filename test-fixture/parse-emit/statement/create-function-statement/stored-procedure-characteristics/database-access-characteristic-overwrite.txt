CREATE FUNCTION FOO ()
    RETURNS BOOLEAN
    MODIFIES SQL DATA
    NO SQL
    RETURN 1;
CREATE FUNCTION FOO ()
    RETURNS BOOLEAN
    NO SQL
    CONTAINS SQL
    RETURN 1;
CREATE FUNCTION FOO ()
    RETURNS BOOLEAN
    CONTAINS SQL
    READS SQL DATA
    RETURN 1;
CREATE FUNCTION FOO ()
    RETURNS BOOLEAN
    READS SQL DATA
    MODIFIES SQL DATA
    RETURN 1;

-----
CREATE DEFINER = CURRENT_USER() FUNCTION `FOO` ()
    RETURNS BOOLEAN
    NOT DETERMINISTIC
    NO SQL
    SQL SECURITY DEFINER
    RETURN 1;

CREATE DEFINER = CURRENT_USER() FUNCTION `FOO` ()
    RETURNS BOOLEAN
    NOT DETERMINISTIC
    CONTAINS SQL
    SQL SECURITY DEFINER
    RETURN 1;

CREATE DEFINER = CURRENT_USER() FUNCTION `FOO` ()
    RETURNS BOOLEAN
    NOT DETERMINISTIC
    READS SQL DATA
    SQL SECURITY DEFINER
    RETURN 1;

CREATE DEFINER = CURRENT_USER() FUNCTION `FOO` ()
    RETURNS BOOLEAN
    NOT DETERMINISTIC
    MODIFIES SQL DATA
    SQL SECURITY DEFINER
    RETURN 1;

-----
[]
