CREATE DEFINER = xxx@yyy
    FUNCTION FOO (XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX BOOLEAN, Y BOOLEAN) RETURNS BOOLEAN RETURN 1;

-----
CREATE DEFINER = `xxx`@yyy FUNCTION `FOO` (
    `XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX` BOOLEAN,
    `Y` BOOLEAN
)
    RETURNS BOOLEAN
    NOT DETERMINISTIC
    SQL SECURITY DEFINER
    RETURN 1;

-----
[]
