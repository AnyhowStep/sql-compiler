CREATE FUNCTION FOO (X BOOLEAN) RETURNS BOOLEAN RETURN 1;

-----
CREATE DEFINER = CURRENT_USER() FUNCTION `FOO` (`X` BOOLEAN)
    RETURNS BOOLEAN
    NOT DETERMINISTIC
    SQL SECURITY DEFINER
    RETURN 1;

-----
[]
