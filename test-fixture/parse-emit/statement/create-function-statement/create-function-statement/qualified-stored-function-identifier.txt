CREATE FUNCTION FOO.W () RETURNS BOOLEAN RETURN 1;

-----
CREATE DEFINER = CURRENT_USER() FUNCTION `FOO`.`W` ()
    RETURNS BOOLEAN
    NOT DETERMINISTIC
    SQL SECURITY DEFINER
    RETURN 1;

-----
[]
