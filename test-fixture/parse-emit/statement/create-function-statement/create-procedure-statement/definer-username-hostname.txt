CREATE DEFINER = xxx@yyy PROCEDURE FOO () RETURN 1;
CREATE DEFINER = xxx@`yyy` PROCEDURE FOO () RETURN 1;
CREATE DEFINER = xxx@'yyy' PROCEDURE FOO () RETURN 1;

-----
CREATE DEFINER = `xxx`@yyy PROCEDURE `FOO` ()
    NOT DETERMINISTIC
    SQL SECURITY DEFINER
    RETURN 1;

CREATE DEFINER = `xxx`@`yyy` PROCEDURE `FOO` ()
    NOT DETERMINISTIC
    SQL SECURITY DEFINER
    RETURN 1;

CREATE DEFINER = `xxx`@'yyy' PROCEDURE `FOO` ()
    NOT DETERMINISTIC
    SQL SECURITY DEFINER
    RETURN 1;

-----
[]
