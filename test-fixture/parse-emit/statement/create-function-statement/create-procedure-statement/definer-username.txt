CREATE DEFINER = xxx PROCEDURE FOO () RETURN 1;
CREATE DEFINER = `xxx` PROCEDURE FOO () RETURN 1;
CREATE DEFINER = 'xxx' PROCEDURE FOO () RETURN 1;

-----
CREATE DEFINER = `xxx`@'%' PROCEDURE `FOO` ()
    NOT DETERMINISTIC
    SQL SECURITY DEFINER
    RETURN 1;

CREATE DEFINER = `xxx`@'%' PROCEDURE `FOO` ()
    NOT DETERMINISTIC
    SQL SECURITY DEFINER
    RETURN 1;

CREATE DEFINER = 'xxx'@'%' PROCEDURE `FOO` ()
    NOT DETERMINISTIC
    SQL SECURITY DEFINER
    RETURN 1;

-----
[]
