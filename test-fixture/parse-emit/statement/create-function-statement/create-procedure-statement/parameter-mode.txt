CREATE PROCEDURE FOO (X BOOLEAN, IN Y BOOLEAN, OUT Z BOOLEAN, INOUT W BOOLEAN) RETURN 1;

-----
CREATE DEFINER = CURRENT_USER() PROCEDURE `FOO` (
    IN `X` BOOLEAN,
    IN `Y` BOOLEAN,
    OUT `Z` BOOLEAN,
    INOUT `W` BOOLEAN
)
    NOT DETERMINISTIC
    SQL SECURITY DEFINER
    RETURN 1;

-----
[]
