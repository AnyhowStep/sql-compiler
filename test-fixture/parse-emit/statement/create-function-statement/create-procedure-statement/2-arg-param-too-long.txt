CREATE DEFINER = xxx@yyy
    PROCEDURE FOO (XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX BOOLEAN, Y BOOLEAN) RETURN 1;

-----
CREATE DEFINER = `xxx`@yyy PROCEDURE `FOO` (
    IN `XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX` BOOLEAN,
    IN `Y` BOOLEAN
)
    NOT DETERMINISTIC
    SQL SECURITY DEFINER
    RETURN 1;

-----
[]
