CREATE USER FOO
    WITH
        MAX_QUERIES_PER_HOUR 0;

CREATE USER FOO
    WITH
        MAX_QUERIES_PER_HOUR 0
        MAX_UPDATES_PER_HOUR 1;

-----
CREATE USER
    `FOO`@'%' IDENTIFIED
        BY ''
    REQUIRE NONE
    ACCOUNT UNLOCK
    PASSWORD EXPIRE DEFAULT;

CREATE USER
    `FOO`@'%' IDENTIFIED
        BY ''
    REQUIRE NONE
    WITH
        MAX_UPDATES_PER_HOUR 1
    ACCOUNT UNLOCK
    PASSWORD EXPIRE DEFAULT;

-----
[]
