CREATE USER FOO
    WITH
        MAX_USER_CONNECTIONS 0;

CREATE USER FOO
    WITH
        MAX_USER_CONNECTIONS 0
        MAX_CONNECTIONS_PER_HOUR 1;

-----
CREATE USER
    `FOO`@'%' IDENTIFIED
        BY ''
    REQUIRE NONE
    ACCOUNT UNLOCK
    PASSWORD EXPIRE DEFAULT;

CREATE USER
    `FOO`@'%' IDENTIFIED
        BY ''
    REQUIRE NONE
    WITH
        MAX_CONNECTIONS_PER_HOUR 1
    ACCOUNT UNLOCK
    PASSWORD EXPIRE DEFAULT;

-----
[]
