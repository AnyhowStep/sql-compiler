CREATE USER FOO
    WITH
        MAX_UPDATES_PER_HOUR 0;

CREATE USER FOO
    WITH
        MAX_UPDATES_PER_HOUR 0
        MAX_USER_CONNECTIONS 1;

-----
CREATE USER
    `FOO`@'%' IDENTIFIED
        BY ''
    REQUIRE NONE
    ACCOUNT UNLOCK
    PASSWORD EXPIRE DEFAULT;

CREATE USER
    `FOO`@'%' IDENTIFIED
        BY ''
    REQUIRE NONE
    WITH
        MAX_USER_CONNECTIONS 1
    ACCOUNT UNLOCK
    PASSWORD EXPIRE DEFAULT;

-----
[]
