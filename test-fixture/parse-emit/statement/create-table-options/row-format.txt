CREATE TABLE T (
    X BOOLEAN
)
    ROW_FORMAT DEFAULT;

CREATE TABLE T (
    X BOOLEAN
)
    ROW_FORMAT FIXED;

CREATE TABLE T (
    X BOOLEAN
)
    ROW_FORMAT DYNAMIC;

CREATE TABLE T (
    X BOOLEAN
)
    ROW_FORMAT COMPRESSED;

CREATE TABLE T (
    X BOOLEAN
)
    ROW_FORMAT REDUNDANT;

CREATE TABLE T (
    X BOOLEAN
)
    ROW_FORMAT COMPACT;

-----
CREATE TABLE `T` (
    `X` BOOLEAN NULL
)
    ROW_FORMAT = DEFAULT;

CREATE TABLE `T` (
    `X` BOOLEAN NULL
)
    ROW_FORMAT = FIXED;

CREATE TABLE `T` (
    `X` BOOLEAN NULL
)
    ROW_FORMAT = DYNAMIC;

CREATE TABLE `T` (
    `X` BOOLEAN NULL
)
    ROW_FORMAT = COMPRESSED;

CREATE TABLE `T` (
    `X` BOOLEAN NULL
)
    ROW_FORMAT = REDUNDANT;

CREATE TABLE `T` (
    `X` BOOLEAN NULL
)
    ROW_FORMAT = COMPACT;

-----
[]
