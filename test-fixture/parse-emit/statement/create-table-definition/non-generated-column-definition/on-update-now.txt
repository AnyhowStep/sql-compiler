CREATE TABLE T (
    X DATETIME ON UPDATE NOW()
);

CREATE TABLE T (
    X DATETIME(0) ON UPDATE NOW(0)
);

CREATE TABLE T (
    X DATETIME(1) ON UPDATE NOW(1)
);

CREATE TABLE T (
    X DATETIME ON UPDATE CURRENT_TIMESTAMP
);

CREATE TABLE T (
    X DATETIME ON UPDATE CURRENT_TIMESTAMP()
);

CREATE TABLE T (
    X DATETIME(0) ON UPDATE CURRENT_TIMESTAMP(0)
);

CREATE TABLE T (
    X DATETIME(1) ON UPDATE CURRENT_TIMESTAMP(1)
);

-----
CREATE TABLE `T` (
    `X` DATETIME(0) NULL
        ON UPDATE CURRENT_TIMESTAMP(0)
);

CREATE TABLE `T` (
    `X` DATETIME(0) NULL
        ON UPDATE CURRENT_TIMESTAMP(0)
);

CREATE TABLE `T` (
    `X` DATETIME(1) NULL
        ON UPDATE CURRENT_TIMESTAMP(1)
);

CREATE TABLE `T` (
    `X` DATETIME(0) NULL
        ON UPDATE CURRENT_TIMESTAMP(0)
);

CREATE TABLE `T` (
    `X` DATETIME(0) NULL
        ON UPDATE CURRENT_TIMESTAMP(0)
);

CREATE TABLE `T` (
    `X` DATETIME(0) NULL
        ON UPDATE CURRENT_TIMESTAMP(0)
);

CREATE TABLE `T` (
    `X` DATETIME(1) NULL
        ON UPDATE CURRENT_TIMESTAMP(1)
);

-----
[]
