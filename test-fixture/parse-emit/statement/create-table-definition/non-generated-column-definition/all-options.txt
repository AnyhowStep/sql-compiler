CREATE TABLE T (
    `X` MEDIUMINT UNSIGNED NOT NULL AUTO_INCREMENT UNIQUE KEY PRIMARY KEY
        COLUMN_FORMAT FIXED
        STORAGE MEMORY
        DEFAULT 'ha'
        ON UPDATE CURRENT_TIMESTAMP
        COMMENT 'lol'
        CHECK (1)
);

-----
CREATE TABLE `T` (
    `X` MEDIUMINT UNSIGNED NOT NULL AUTO_INCREMENT UNIQUE KEY PRIMARY KEY
        COLUMN_FORMAT FIXED
        STORAGE MEMORY
        DEFAULT 'ha'
        ON UPDATE CURRENT_TIMESTAMP(0)
        COMMENT 'lol'
        CHECK (1)
);

-----
[]
