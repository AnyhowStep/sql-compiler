CREATE TABLE T (
    X BOOL,
    CONSTRAINT CFK FOREIGN KEY FK (X) REFERENCES U (X)
);

-----
CREATE TABLE `T` (
    `X` BOOLEAN NULL,
    CONSTRAINT `CFK` FOREIGN KEY `CFK` (`X`) REFERENCES `U` (`X`)
);

-----
[]
