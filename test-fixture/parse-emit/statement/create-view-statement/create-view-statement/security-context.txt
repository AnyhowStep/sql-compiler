CREATE SQL SECURITY DEFINER VIEW FOO AS
    SELECT
        1;

CREATE SQL SECURITY INVOKER VIEW FOO AS
    SELECT
        1;

-----
CREATE
    DEFINER = CURRENT_USER()
    SQL SECURITY DEFINER
    VIEW `FOO` AS
    SELECT
        1;

CREATE
    DEFINER = CURRENT_USER()
    SQL SECURITY INVOKER
    VIEW `FOO` AS
    SELECT
        1;

-----
[]
