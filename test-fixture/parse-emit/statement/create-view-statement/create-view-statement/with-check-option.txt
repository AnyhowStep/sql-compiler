CREATE VIEW FOO AS
    SELECT
        1
    WITH CHECK OPTION;

CREATE VIEW FOO AS
    SELECT
        1
    WITH CASCADED CHECK OPTION;

CREATE VIEW FOO AS
    SELECT
        1
    WITH LOCAL CHECK OPTION;

-----
CREATE
    DEFINER = CURRENT_USER()
    SQL SECURITY DEFINER
    VIEW `FOO` AS
    SELECT
        1
    WITH CASCADED CHECK OPTION;

CREATE
    DEFINER = CURRENT_USER()
    SQL SECURITY DEFINER
    VIEW `FOO` AS
    SELECT
        1
    WITH CASCADED CHECK OPTION;

CREATE
    DEFINER = CURRENT_USER()
    SQL SECURITY DEFINER
    VIEW `FOO` AS
    SELECT
        1
    WITH LOCAL CHECK OPTION;

-----
[]
