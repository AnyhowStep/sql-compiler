CREATE ALGORITHM = MERGE VIEW FOO AS
    SELECT
        1;

CREATE ALGORITHM = TEMPTABLE VIEW FOO AS
    SELECT
        1;

CREATE ALGORITHM = UNDEFINED VIEW FOO AS
    SELECT
        1;

-----
CREATE
    ALGORITHM = MERGE
    DEFINER = CURRENT_USER()
    SQL SECURITY DEFINER
    VIEW `FOO` AS
    SELECT
        1;

CREATE
    ALGORITHM = TEMPTABLE
    DEFINER = CURRENT_USER()
    SQL SECURITY DEFINER
    VIEW `FOO` AS
    SELECT
        1;

CREATE
    ALGORITHM = UNDEFINED
    DEFINER = CURRENT_USER()
    SQL SECURITY DEFINER
    VIEW `FOO` AS
    SELECT
        1;

-----
[]
