CREATE TABLE T (
    X BOOLEAN
)
    PARTITION BY RANGE COLUMNS(`X`, `Y`)
    PARTITIONS 1 (
        PARTITION a VALUES LESS THAN (1, 2)
    );

-----
CREATE TABLE `T` (
    `X` BOOLEAN NULL
)
    PARTITION BY RANGE COLUMNS(`X`, `Y`)
    PARTITIONS 1 (
        PARTITION `a` VALUES LESS THAN (1, 2)
    );

-----
[]
