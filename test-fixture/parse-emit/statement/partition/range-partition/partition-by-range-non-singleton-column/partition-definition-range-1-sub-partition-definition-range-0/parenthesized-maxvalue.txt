CREATE TABLE T (
    X BOOLEAN
)
    PARTITION BY RANGE COLUMNS(`X`, `Y`) (
        PARTITION a VALUES LESS THAN (MAXVALUE, MAXVALUE)
    );

-----
CREATE TABLE `T` (
    `X` BOOLEAN NULL
)
    PARTITION BY RANGE COLUMNS(`X`, `Y`) (
        PARTITION `a` VALUES LESS THAN (MAXVALUE, MAXVALUE)
    );

-----
[]
