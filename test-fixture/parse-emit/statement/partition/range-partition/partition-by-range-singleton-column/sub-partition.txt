CREATE TABLE T (
    X BOOLEAN
)
    PARTITION BY RANGE COLUMNS(`X`)
    SUBPARTITION BY HASH (32) (
        PARTITION a VALUES LESS THAN (1)
    );

CREATE TABLE T (
    X BOOLEAN
)
    PARTITION BY RANGE COLUMNS(`X`)
    SUBPARTITION BY LINEAR HASH (32)
    SUBPARTITIONS 5 (
        PARTITION a VALUES LESS THAN (1)
    );

CREATE TABLE T (
    X BOOLEAN
)
    PARTITION BY RANGE COLUMNS(`X`)
    SUBPARTITION BY KEY (X) (
        PARTITION a VALUES LESS THAN (1)
    );

CREATE TABLE T (
    X BOOLEAN
)
    PARTITION BY RANGE COLUMNS(`X`)
    SUBPARTITION BY LINEAR KEY ALGORITHM = 1 (X)
    SUBPARTITIONS 5 (
        PARTITION a VALUES LESS THAN (1)
    );

-----
CREATE TABLE `T` (
    `X` BOOLEAN NULL
)
    PARTITION BY RANGE COLUMNS(`X`)
    SUBPARTITION BY HASH (32) (
        PARTITION `a` VALUES LESS THAN (1)
    );

CREATE TABLE `T` (
    `X` BOOLEAN NULL
)
    PARTITION BY RANGE COLUMNS(`X`)
    SUBPARTITION BY LINEAR HASH (32)
    SUBPARTITIONS 5 (
        PARTITION `a` VALUES LESS THAN (1)
    );

CREATE TABLE `T` (
    `X` BOOLEAN NULL
)
    PARTITION BY RANGE COLUMNS(`X`)
    SUBPARTITION BY KEY (`X`) (
        PARTITION `a` VALUES LESS THAN (1)
    );

CREATE TABLE `T` (
    `X` BOOLEAN NULL
)
    PARTITION BY RANGE COLUMNS(`X`)
    SUBPARTITION BY LINEAR KEY ALGORITHM = 1 (`X`)
    SUBPARTITIONS 5 (
        PARTITION `a` VALUES LESS THAN (1)
    );

-----
[]
