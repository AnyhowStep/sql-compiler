CREATE TABLE T (
    X BOOLEAN
)
    PARTITION BY LINEAR KEY ALGORITHM = 2 (X) PARTITIONS 5;

-----
CREATE TABLE `T` (
    `X` BOOLEAN NULL
)
    PARTITION BY LINEAR KEY ALGORITHM = 2 (`X`)
    PARTITIONS 5;

-----
[]
