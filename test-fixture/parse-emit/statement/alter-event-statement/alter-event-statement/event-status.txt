ALTER EVENT FOO
    ENABLE;

ALTER EVENT FOO
    DISABLE;

ALTER EVENT FOO
    DISABLE ON SLAVE;

-----
ALTER DEFINER = CURRENT_USER() EVENT `FOO`
    ENABLE;

ALTER DEFINER = CURRENT_USER() EVENT `FOO`
    DISABLE;

ALTER DEFINER = CURRENT_USER() EVENT `FOO`
    DISABLE ON SLAVE;

-----
[]
