SELECT
    *
FROM
    ({oj T})
    INNER JOIN
    U;

SELECT
    *
FROM
    (({oj T}))
    INNER JOIN
    U;

SELECT
    *
FROM
    ((({oj T})))
    INNER JOIN
    U;

-----
SELECT
    *
FROM
    ({oj `T`})
    INNER JOIN
    `U`;

SELECT
    *
FROM
    ({oj `T`})
    INNER JOIN
    `U`;

SELECT
    *
FROM
    ({oj `T`})
    INNER JOIN
    `U`;

-----
[]
