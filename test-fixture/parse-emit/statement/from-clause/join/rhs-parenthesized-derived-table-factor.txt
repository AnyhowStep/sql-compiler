SELECT
    *
FROM
    T
    INNER JOIN
    ((
        SELECT
            *
        FROM
            U
    ) AS tmp);

SELECT
    *
FROM
    T
    INNER JOIN
    (((
        SELECT
            *
        FROM
            U
    ) AS tmp));

SELECT
    *
FROM
    T
    INNER JOIN
    ((((
        SELECT
            *
        FROM
            U
    ) AS tmp)));

-----
SELECT
    *
FROM
    `T`
    INNER JOIN
    (
        SELECT
            *
        FROM
            `U`
    ) AS `tmp`;

SELECT
    *
FROM
    `T`
    INNER JOIN
    (
        SELECT
            *
        FROM
            `U`
    ) AS `tmp`;

SELECT
    *
FROM
    `T`
    INNER JOIN
    (
        SELECT
            *
        FROM
            `U`
    ) AS `tmp`;

-----
[]
