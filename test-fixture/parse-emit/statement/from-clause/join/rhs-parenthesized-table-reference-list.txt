SELECT
    *
FROM
    T
    INNER JOIN
    ((U, V));

SELECT
    *
FROM
    T
    INNER JOIN
    (((U, V)));

SELECT
    *
FROM
    T
    INNER JOIN
    ((((U, V))));

-----
SELECT
    *
FROM
    `T`
    INNER JOIN
    (
        `U`,
        `V`
    );

SELECT
    *
FROM
    `T`
    INNER JOIN
    (
        `U`,
        `V`
    );

SELECT
    *
FROM
    `T`
    INNER JOIN
    (
        `U`,
        `V`
    );

-----
[]
