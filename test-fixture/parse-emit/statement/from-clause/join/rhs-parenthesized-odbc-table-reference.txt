SELECT
    *
FROM
    T
    INNER JOIN
    ({oj U});

SELECT
    *
FROM
    T
    INNER JOIN
    (({oj U}));

SELECT
    *
FROM
    T
    INNER JOIN
    ((({oj U})));

-----
SELECT
    *
FROM
    `T`
    INNER JOIN
    ({oj `U`});

SELECT
    *
FROM
    `T`
    INNER JOIN
    ({oj `U`});

SELECT
    *
FROM
    `T`
    INNER JOIN
    ({oj `U`});

-----
[]
