SELECT
    *
FROM
    ((T, U))
    INNER JOIN
    V;

SELECT
    *
FROM
    (((T, U)))
    INNER JOIN
    V;

SELECT
    *
FROM
    ((((T, U))))
    INNER JOIN
    V;

-----
SELECT
    *
FROM
    (
        `T`,
        `U`
    )
    INNER JOIN
    `V`;

SELECT
    *
FROM
    (
        `T`,
        `U`
    )
    INNER JOIN
    `V`;

SELECT
    *
FROM
    (
        `T`,
        `U`
    )
    INNER JOIN
    `V`;

-----
[]
