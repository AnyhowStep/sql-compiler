SELECT
    *
FROM
    (T
    CROSS JOIN
    U)
    CROSS JOIN
    V;

SELECT
    *
FROM
    ((T
    CROSS JOIN
    U))
    CROSS JOIN
    V;

SELECT
    *
FROM
    (((T
    CROSS JOIN
    U)))
    CROSS JOIN
    V;

-----
SELECT
    *
FROM
    `T`
    CROSS JOIN
    `U`
    CROSS JOIN
    `V`;

SELECT
    *
FROM
    `T`
    CROSS JOIN
    `U`
    CROSS JOIN
    `V`;

SELECT
    *
FROM
    `T`
    CROSS JOIN
    `U`
    CROSS JOIN
    `V`;

-----
[]
