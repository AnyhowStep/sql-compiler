SELECT
    *
FROM
    T
    INNER JOIN
    (U);

SELECT
    *
FROM
    T
    INNER JOIN
    ((U));

SELECT
    *
FROM
    T
    INNER JOIN
    (((U)));

-----
SELECT
    *
FROM
    `T`
    INNER JOIN
    `U`;

SELECT
    *
FROM
    `T`
    INNER JOIN
    `U`;

SELECT
    *
FROM
    `T`
    INNER JOIN
    `U`;

-----
[]
