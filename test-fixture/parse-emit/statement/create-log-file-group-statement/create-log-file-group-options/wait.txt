CREATE LOGFILE GROUP x
    ADD UNDOFILE 'y'
    WAIT;

CREATE LOGFILE GROUP x
    ADD UNDOFILE 'y'
    NO_WAIT;

-----
CREATE LOGFILE GROUP `x`
    ADD UNDOFILE 'y'
    INITIAL_SIZE = `128M`
    UNDO_BUFFER_SIZE = `8M`
    WAIT;

CREATE LOGFILE GROUP `x`
    ADD UNDOFILE 'y'
    INITIAL_SIZE = `128M`
    UNDO_BUFFER_SIZE = `8M`
    NO_WAIT;

-----
[]
