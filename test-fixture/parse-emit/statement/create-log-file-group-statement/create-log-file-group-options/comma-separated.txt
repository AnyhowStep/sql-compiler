CREATE LOGFILE GROUP x
    ADD UNDOFILE 'y'
    COMMENT 'hi',
    ENGINE InnoDB;

-----
CREATE LOGFILE GROUP `x`
    ADD UNDOFILE 'y'
    INITIAL_SIZE = `128M`
    UNDO_BUFFER_SIZE = `8M`
    ENGINE = `InnoDB`
    COMMENT = 'hi';

-----
[]
