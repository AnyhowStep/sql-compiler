CREATE LOGFILE GROUP x
    ADD UNDOFILE 'y'
    ENGINE = a;

CREATE LOGFILE GROUP x
    ADD UNDOFILE 'y'
    ENGINE = 'a';

-----
CREATE LOGFILE GROUP `x`
    ADD UNDOFILE 'y'
    INITIAL_SIZE = `128M`
    UNDO_BUFFER_SIZE = `8M`
    ENGINE = `a`;

CREATE LOGFILE GROUP `x`
    ADD UNDOFILE 'y'
    INITIAL_SIZE = `128M`
    UNDO_BUFFER_SIZE = `8M`
    ENGINE = 'a';

-----
[]
