CREATE LOGFILE GROUP x
    ADD UNDOFILE 'y'
    INITIAL_SIZE 123;

CREATE LOGFILE GROUP x
    ADD UNDOFILE 'y'
    INITIAL_SIZE 123M;

-----
CREATE LOGFILE GROUP `x`
    ADD UNDOFILE 'y'
    INITIAL_SIZE = 123
    UNDO_BUFFER_SIZE = `8M`;

CREATE LOGFILE GROUP `x`
    ADD UNDOFILE 'y'
    INITIAL_SIZE = `123M`
    UNDO_BUFFER_SIZE = `8M`;

-----
[]
