CREATE LOGFILE GROUP x
    ADD UNDOFILE 'y'
    STORAGE ENGINE = a;

CREATE LOGFILE GROUP x
    ADD UNDOFILE 'y'
    STORAGE ENGINE = 'a';

-----
CREATE LOGFILE GROUP `x`
    ADD UNDOFILE 'y'
    INITIAL_SIZE = `128M`
    UNDO_BUFFER_SIZE = `8M`
    ENGINE = `a`;

CREATE LOGFILE GROUP `x`
    ADD UNDOFILE 'y'
    INITIAL_SIZE = `128M`
    UNDO_BUFFER_SIZE = `8M`
    ENGINE = 'a';

-----
[]
