CREATE FUNCTION FOO ()
    RETURNS BOOLEAN
    a: LOOP
        RETURN 1;
    END LOOP;

-----
CREATE DEFINER = CURRENT_USER() FUNCTION `FOO` ()
    RETURNS BOOLEAN
    NOT DETERMINISTIC
    SQL SECURITY DEFINER
    `a`: LOOP
        RETURN 1;
    END LOOP;

-----
[]
