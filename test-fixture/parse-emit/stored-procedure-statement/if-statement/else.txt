CREATE FUNCTION FOO ()
    RETURNS BOOLEAN
    IF 1 THEN
        RETURN 1;
    ELSE
        RETURN '';
    END IF;

-----
CREATE DEFINER = CURRENT_USER() FUNCTION `FOO` ()
    RETURNS BOOLEAN
    NOT DETERMINISTIC
    SQL SECURITY DEFINER
    IF 1 THEN
        RETURN 1;
    ELSE
        RETURN '';
    END IF;

-----
[]
