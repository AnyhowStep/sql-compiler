CREATE FUNCTION FOO ()
    RETURNS BOOLEAN
    REPEAT
        RETURN 1;
    UNTIL 1
    END REPEAT;

-----
CREATE DEFINER = CURRENT_USER() FUNCTION `FOO` ()
    RETURNS BOOLEAN
    NOT DETERMINISTIC
    SQL SECURITY DEFINER
    REPEAT
        RETURN 1;
    UNTIL 1
    END REPEAT;

-----
[]
