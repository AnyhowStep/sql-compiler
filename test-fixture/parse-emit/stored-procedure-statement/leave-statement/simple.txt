CREATE FUNCTION FOO ()
    RETURNS BOOLEAN
    a: BEGIN
        LEAVE a;
        RETURN 1;
    END;

-----
CREATE DEFINER = CURRENT_USER() FUNCTION `FOO` ()
    RETURNS BOOLEAN
    NOT DETERMINISTIC
    SQL SECURITY DEFINER
    `a`: BEGIN
        LEAVE `a`;

        RETURN 1;
    END;

-----
[]
