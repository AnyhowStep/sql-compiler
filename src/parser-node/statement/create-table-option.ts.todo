import {Node} from "../node";

export interface TableOption extends Node {
    autoIncrement;
    averageRowLength;
    characterSet;
    checksum;
    collate;
    comment;
    compression;
    connection;
    dataDirectory;
    indexDirectory;
    delayKeyWrite;
    encryption;
    engine;
    insertMethod;
    keyBlockSize;
    maxRows;
    minRows;
    packKeys;
    password;
    rowFormat;
    statsAutoRecalc;
    statsPersistent;
    statsSamplePages;
    tablespace;
    union;
}
